"use client";

/**
 * Dashboard page — the actual rendering lives in PersistentDashboard
 * (mounted in the layout) so widgets survive sidebar navigation.
 * This page is intentionally empty; it just needs to exist so
 * Next.js routes to /dashboard and PersistentDashboard shows itself.
 */
export default function DashboardPage() {
  return null;
}
